1. $> cd ~/initiatives-flowchart-poc/vanilla-js-state-management

2. $> node flow-algo.js > stateOD.txt

3. quote your string in stateOD.txt file, so that it becomes a single argument:
	$> ~/compSc/quoteStr.sh [file]

Output in file can now be used to consume in file cell.js


