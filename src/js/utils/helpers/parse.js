export default function parse(inputHtml) {
  const tagsType = {
	htmlTag: '<[a-z]\w*?>',
	customComponent: '<[a-zA-Z]\w*?>'
  }

  inputHtml.exec(inputHtml);

  if (htmlTag) {
	doument.createElement();
  }
}
